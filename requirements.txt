# PyInstaller GUI 项目依赖
# 基于项目代码分析生成的完整依赖列表

# ==================== 核心依赖 ====================
# GUI框架
PyQt5>=5.15.0

# 打包工具
pyinstaller>=5.13.0

# ==================== 必需依赖 ====================
# 模块导入和元数据处理
importlib-metadata>=4.0.0

# 图像处理（用于图标和界面）
Pillow>=8.0.0

# 类型检查和注解支持
typing-extensions>=4.0.0

# ==================== 可选依赖（增强功能） ====================
# 用于高级依赖检测和分析
setuptools>=65.0.0

# 用于处理包信息（Python 3.8+内置，无需单独安装）
# pkg-resources 已集成到 setuptools 中

# ==================== 开发和测试依赖 ====================
# 用于测试网络功能（可选）
# requests>=2.28.0

# 用于数据处理测试（可选）
# pandas>=1.5.0
# numpy>=1.21.0

# ==================== 平台特定依赖 ====================
# Windows平台依赖
pywin32>=306; sys_platform=="win32"

# ==================== 说明 ====================
# 1. 核心依赖是运行应用程序必需的
# 2. 必需依赖用于应用程序的完整功能
# 3. 可选依赖可以根据需要安装
# 4. 开发依赖仅在开发和测试时需要
# 5. 平台特定依赖仅在特定操作系统上需要

# 安装命令：
# pip install -r requirements.txt

# 仅安装核心依赖：
# pip install PyQt5>=5.15.0 pyinstaller>=5.13.0

# 完整安装（包括可选依赖）：
# pip install -r requirements.txt requests pandas numpy